apiVersion: v1
kind: Service
metadata:
  name: flask-test-service
spec:
  selector:
    app: flask-test
  ports:
    - protocol: TCP
      port: 80        # internal cluster port
      targetPort: 5000  # Flask container port
      nodePort: 30080   # external fixed port
  type: NodePort


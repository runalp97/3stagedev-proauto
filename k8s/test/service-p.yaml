apiVersion: v1
kind: Service
metadata:
  name: flask-test-service
  labels:
    app: flask-test
spec:
  selector:
    app: flask-test
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP
